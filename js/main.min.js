document.querySelectorAll(".maincard__body-item").forEach((el) => {
    el.addEventListener('click', ()=> {
        let cont = el.querySelector('.maincard__body-item-links');
        let con = cont.parentNode;
        if (cont.style.maxHeight) {
            document.querySelectorAll('.maincard__body-item-links').forEach((el)=> el.style.maxHeight = null)
            document.querySelectorAll('.maincard__body-item').forEach((el)=> el.classList.remove('open'))
        } else {
            document.querySelectorAll('.maincard__body-item-links').forEach((el)=> el.style.maxHeight = null)
            document.querySelectorAll('.maincard__body-item').forEach((el)=> el.classList.remove('open'))
            con.classList.add('open')
            cont.style.maxHeight = cont.scrollHeight + 'rem';
        }
    })
})
$('.link__city').on('mouseup', function(e){
  let ChangeCity = $('#city__desk').text()
  let ReceiveCity = $(this).text()
  if ($(this).attr('data-city') == 1){
    $('#number__desk').text('(861) 266-69-54')
    $('.tel').attr('href', '8612666954')
    $(this).text(`${ChangeCity}`)
    $('#city__desk').text(`${ReceiveCity}`)
  }else{
    $('#number__desk').text('(8652) 50-01-84')
    $('.tel').attr('href', '8652500184')
    $(this).text(`${ChangeCity}`)
    $('#city__desk').text(`${ReceiveCity}`)
  }
})
$('.link__city-mob').on('mouseup', function(e){
  let ChangeCity = $('#city__mob').text()
  let ReceiveCity = $(this).text()
  if ($(this).attr('data-cityMob') == 1){
    $('#number__mobile').text('(861) 266-69-54')
    $('.tel').attr('href', '8612666954')
    $(this).text(`${ChangeCity}`)
    $('#city__mob').text(`${ReceiveCity}`)
  }else{
    $('#number__mobile').text('(8652) 50-01-84')
    $('.tel').attr('href', '8652500184')
    $(this).text(`${ChangeCity}`)
    $('#city__mob').text(`${ReceiveCity}`)
  }
})
  $(".shin__tab").on("click", function (e) {
    $(this).addClass("active"),
      1 == $(this).attr("data-tab")
        ? ($(".text__one").removeClass("none"),
          $(".text__two").addClass("none"))
        : ($(".text__two").removeClass("none"),
          $(".text__one").addClass("none")),
      $(".shin__tab").not(this).removeClass("active");
  }),
  jQuery(function (e) {
    e(document).mouseup(function (t) {
      e(".open__modal").is(t.target) ||
      0 !== e(".open__modal").has(t.target).length
        ? (e(".modal").addClass("active"), e("body").addClass("hidden"))
        : (e(".modal__body").is(t.target) ||
            0 !== e(".modal__body").has(t.target).length ||
            (e(".modal").removeClass("active"),
            e("body").removeClass("hidden")),
          e(".close__modal").is(t.target) &&
            0 === e(".close__modal").has(t.target).length &&
            (e(".modal").removeClass("active"),
            e("body").removeClass("hidden")));
    });
  }),
  $(".burger").on("click", function (e) {
    $(".header__mobile").hasClass("active")
      ? ($(".header__mobile").removeClass("active"),
        $(".body").removeClass("hidden"),
        $(this).removeClass("open"))
      : ($(".header__mobile").addClass("active"),
        $(".body").addClass("hidden"),
        $(this).addClass("open"));
  }),
  $(".header__mobile-bot-link-ar").on("click", function (e) {
    $(this).hasClass("active")
      ? $(this).removeClass("active")
      : $(this).addClass("active"),
      $(".header__mobile-bot-link-ar").not(this).removeClass("active");
  });
  $(".form__link").on('click', function(e){
    $(".header__mobile").removeClass("active")
    $(".body").removeClass("hidden")
  })
var splide = new Splide(".splide", { type: "loop" });
splide.mount();

